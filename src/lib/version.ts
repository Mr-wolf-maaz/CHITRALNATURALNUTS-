/**
 * Build version — baked into both the client bundle and server responses.
 * If the client's copy doesn't match the server's, the browser is running a
 * stale bundle and the UI forces a reload instead of failing mysteriously.
 */
export const APP_VERSION = "2026.09.01-auth4";
